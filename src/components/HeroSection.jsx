import React from "react";
import "./Header.css";
import Header from "./Header.jsx";

const HeroSection = () => {
    return (
        <div className="hero-container">
            <div className="hero-overlay">
                <h1 className="hero-title">Events planen - so einfach wie nie zuvor</h1>
            </div>
        </div>
    );
};

export default HeroSection;
